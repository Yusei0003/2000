# 日直勤務表 自動作成アプリ　仕様書

| 項目 | 内容 |
| --- | --- |
| 文書名 | 日直勤務表 自動作成アプリ　仕様書 |
| 版数 | 1.0 |
| 作成日 | 2026-08-19 |
| 対象システム | 日直勤務表 自動作成アプリ |
| 関連文書 | 設計書（`docs/設計書.md`）、README（`README.md`） |
| 出典 | 「日直勤務表の作成及び変更等についてのマニュアル」、追加仕様確認書 |

---

## 1. 目的・背景

### 1.1 目的

本システムは、年2回（前期＝4〜9月、後期＝10〜翌3月）に作成している日直勤務表（土日祝日・年末年始の当番表）の作成業務を自動化し、以下を実現することを目的とする。

- 手作業による割当作業の省力化
- 複数の割当ルール（間隔日数・ペア重複回避・課除外等）の漏れのない適用
- 担当者交代時の引継ぎの容易化

### 1.2 背景

日直勤務表の作成は、職員名簿・過去の勤務実績・行事日程・育休産休情報など複数の情報を突き合わせて行う必要があり、手作業では時間を要するうえ、ルール適用の漏れが生じやすい。また担当者交代のたびに過去の実績データの引継ぎが課題となっていた。

### 1.3 想定利用者

- 日直勤務表の作成を担当する職員（1名）
- 担当交代時の後任職員

---

## 2. 適用範囲

### 2.1 対象業務

| 対象 | 内容 |
| --- | --- |
| 対象とする | 日直勤務表の作成、変更届による交代の反映、過去実績の管理、担当交代時の引継ぎ |
| 対象としない | 日直勤務の実施そのもの、勤怠管理、給与計算、変更届の起案・決裁（別システム） |

### 2.2 対象職員

日直の割当対象は**補佐級以下**の職員とする。以下に該当する職員は割当対象から除外する。

- 課長級以上（人事データのランクにより自動判定）
- 外局等（常時除外所属として指定）
- 秘書係（係名により自動判定）
- 運転手（兼職欄により自動判定）
- 会計年度任用職員（区分名により自動判定）
- 派遣（区分名による自動判定、および名簿上の「派遣（除外）」指定）
- 7割措置（再任用短時間等。名簿上の「7割措置（除外）」指定）
- 育児休業・産前産後休暇等の期間中の職員
- 採用から6ヶ月を経過していない新規採用職員
- 性別が未設定の職員（同性ペアのルール上、割当できないため）

---

## 3. 用語定義

| 用語 | 定義 |
| --- | --- |
| 処理期 | 勤務表を作成する単位となる半期。前期＝4/1〜9/30、後期＝10/1〜翌3/31。「2026年度 前期」のように表す |
| 指定日 | 日直の割当対象となる日。土曜・日曜・祝日・年末年始閉庁日（12/28〜1/3） |
| 係長級 | ペアの一方を構成する級。人事データのランク400〜600 |
| 主事級 | ペアの他方を構成する級。人事データのランク601〜999 |
| ペア | 1日の日直を担当する係長級1名・主事級1名の組合せ |
| 資格要件 | ペアのうち少なくとも1名が係長級または市民課経験者であること |
| 常時除外所属 | 月や行事に関係なく常に割当対象から外す所属 |
| 枯渇ベース | 女性を優先して割り当て、条件を満たす女性候補が枯渇した場合のみ男性に切り替える方式 |
| 要確認 | 制約を緩和して割り当てた日、または割当できなかった日。担当者による確認が必要 |
| 未分類 | 処理期を削除した際に、どの処理期にも紐づかなくなった確定済み履歴 |
| 変更届 | 確定した日直勤務表に対して担当者を交代する際に提出される申請 |

---

## 4. システム概要

### 4.1 システム形態

| 項目 | 内容 |
| --- | --- |
| 形態 | ブラウザ上で動作するスタンドアロンアプリケーション |
| サーバー | 不要 |
| ネットワーク | 不要（外部通信を一切行わない） |
| データ保存先 | 利用端末のブラウザ localStorage |
| 起動方法 | `index.html` をブラウザで開く |

### 4.2 画面構成

画面上部に処理期セレクタを常時表示し、その下に以下6つのタブを配置する。

| タブ | 主な機能 |
| --- | --- |
| 名簿管理 | Excel名簿の取込、職員名簿の確認・編集、職員の個別登録 |
| ルール設定 | 基本設定、常時除外所属、育休・産休等の除外期間、月別の課除外ルール |
| 行事管理 | 年度別の行事日程・除外所属の登録 |
| 勤務表作成 | 指定日の自動抽出、勤務表の自動作成、確定・履歴保存、CSV/PDF出力 |
| 履歴・確認 | 確定済み履歴の確認、変更届の反映、担当実績の確認、各種出力、バックアップ |
| 使い方 | 操作手順とルールの出典 |

---

## 5. 業務フロー

### 5.1 通常の勤務表作成（新しい処理期）

```
1. 処理期を追加する（前処理期の「常時除外する所属」を自動引継ぎ）
        ↓
2. 名簿管理タブで、その処理期のExcel名簿を取り込む
   （前処理期の「市民課経験」「派遣」「7割措置」を職員番号単位で自動引継ぎ）
        ↓
3. ルール設定タブで、育休・産休等の除外期間を登録・取込
        ↓
4. 行事管理タブで、その年度の行事日程・除外所属を登録
        ↓
5. 勤務表作成タブで「土日・祝日・年末年始を自動抽出」
        ↓
6.「勤務表を作成する」で自動割当
        ↓
7. 「要確認」の日と未割当職員を確認し、必要に応じて手動調整
        ↓
8.「確定・履歴に保存」
        ↓
9. CSV・PDFで出力し、通知・掲示
```

### 5.2 変更届の反映

```
1. 変更届を受理する
        ↓
2. 履歴・確認タブで該当日の「交代を反映」を押す
        ↓
3. 変更届のスクリーンショットを読み取る（任意）／交代後の氏名・申請日時を手入力
        ↓
4. 内容を確認して「反映する」
```

### 5.3 過去実績のみの登録

名簿登録や勤務表作成を行わず、過去の日直割だけを登録する場合の流れ。

```
1. 対象年度の処理期を追加する
        ↓
2. 履歴・確認タブの「Excel勤務実績データの取り込み」で過去の勤務表を取り込む
```

### 5.4 担当交代

```
前任者：履歴・確認タブの「引継ぎ用に書き出す」でExcelを出力し、後任者へ受け渡す
        ↓
後任者：自身の端末でExcel名簿を取り込み、続けて引継ぎファイルを
        「Excel勤務実績データの取り込み」で読み込む
        ↓
前任者：ブラウザの閲覧データ削除により localStorage の内容を消去する
```

---

## 6. 機能要件

### 6.1 処理期管理

| No | 機能 | 内容 |
| --- | --- | --- |
| 6.1.1 | 処理期の選択 | 画面上部のセレクタで処理期を切り替える。切替時、名簿・ルール・勤務表作成の対象期間・履歴の表示が当該処理期の内容に切り替わる |
| 6.1.2 | 処理期の追加 | 年度と区分（前期／後期）を指定して作成する。過去の年度も作成可能。直前の処理期の「常時除外する所属」を自動的に引き継ぐ |
| 6.1.3 | 前回処理期からコピー | 直前の処理期の「常時除外する所属」を、現在の処理期へ再度コピーする |
| 6.1.4 | 処理期の削除 | 処理期そのものを削除する。紐づく確定済み履歴は削除せず「未分類」として残し、ペア重複回避・間隔日数の判定には引き続き使用する |
| 6.1.5 | 過去処理期の案内 | 終了日が本日より前の処理期を選択した場合、案内バナーを表示する |

### 6.2 名簿管理

名簿は**処理期ごとに保持**する。処理期を切り替えると、その処理期の名簿が表示される。

| No | 機能 | 内容 |
| --- | --- | --- |
| 6.2.1 | Excel名簿の取込 | 人事給与システム等から出力した名簿（.xlsx）を取り込む。**選択中の処理期の名簿を取込内容で全置換する**（個別登録した職員も置換対象） |
| 6.2.2 | 前処理期からの引継ぎ | 取込時、前処理期に登録されていた同じ職員番号の「市民課経験」「派遣（除外）」「7割措置（除外）」を自動的に引き継ぐ。引継ぎ結果は件数付きで画面に表示する |
| 6.2.3 | 市民課経験の自動判定 | 取込時、現在の所属が市民課の職員は「市民課経験」を自動的にオンにする |
| 6.2.4 | 所属履歴の記録 | 取込時、前処理期と所属が変わっている場合は前処理期の所属を所属履歴に追加する |
| 6.2.5 | 対象者リスト／除外リスト | 名簿を「対象者リスト」（割当対象）と「除外リスト」（秘書係・運転手・派遣・7割措置・常時除外所属・対象外のいずれかに該当）に分けて表示する。除外リストには除外理由を表示する |
| 6.2.6 | 職員検索 | 番号・氏名・所属・係名・職名で名簿を絞り込む |
| 6.2.7 | 個別項目の変更 | 名簿一覧上で「市民課経験」「派遣（除外）」「7割措置（除外）」をチェックボックスで変更する |
| 6.2.8 | 職員の個別登録 | Excel名簿に含まれない職員を個別に追加する。性別は設定できないため、割当対象にはならない |
| 6.2.9 | 職員の削除 | 名簿から職員を削除する。確定済み履歴の表示名は残る |
| 6.2.10 | CSV取込／書出 | 簡易形式のCSVによる取込・書出を行う |
| 6.2.11 | 未取込の処理期の扱い | その処理期にまだ名簿が取り込まれていない場合、対象者リスト・除外リストを含む名簿の一覧そのものを表示せず、取込を促すメッセージのみを表示する |
| 6.2.12 | 処理期の名簿の削除 | 選択中の処理期の名簿をすべて削除し、「未取込」の状態に戻す |

### 6.3 ルール設定

| No | 機能 | 内容 |
| --- | --- | --- |
| 6.3.1 | 基本設定 | 最低間隔日数（既定120日）、新規採用職員の除外期間（既定6ヶ月）、年末年始・GWの遡り回数（既定2回）、ペア重複の遡り年度数（既定2年度） |
| 6.3.2 | 資格要件 | 設定不要。ペアのうち少なくとも1名が係長級または市民課経験者であることを常に適用する |
| 6.3.3 | 常時除外する所属 | 名簿に登録済みの所属からチェックボックスで選択する（所属CD順に表示）。処理期ごとに保持する |
| 6.3.4 | 育休・産休等の除外期間 | 育休一覧・産休一覧のExcel取込、および職員番号・開始日・終了日による個別登録。終了日が未定の場合は空欄で登録でき、復帰まで対象外として扱う |
| 6.3.5 | 除外期間の重複排除 | 同じ職員・同じ期間の行は取込時に自動的にスキップする |
| 6.3.6 | 名簿外職員の除外 | 名簿にない職員番号の行は取込対象外とし、個別登録も受け付けない |
| 6.3.7 | 除外期間の絞り込み表示 | 一覧には選択中の処理期と期間が重なる行のみ表示する |
| 6.3.8 | 月別の課除外ルール | 月と所属の組合せで除外ルールを登録する。既定値として10・11月の商工観光課／広報係、12・1月のふるさと納税係、1〜4月の税務課を設定する |

### 6.4 行事管理

| No | 機能 | 内容 |
| --- | --- | --- |
| 6.4.1 | 行事の登録 | 年度ごとに行事名・行事日・終了日・除外リードタイム（既定30日）・除外する所属を登録する |
| 6.4.2 | 行事テンプレート | 産業まつり・花火大会・選挙・成人式等の候補を入力補助として提供する |
| 6.4.3 | 除外開始日の自動計算 | 行事日とリードタイムから除外開始日を自動計算する |
| 6.4.4 | 前年度の行事をコピー | 前年度の行事名・除外所属・リードタイムを引き継いで作成する（日付は空欄） |
| 6.4.5 | 前年度の登録状況（参考） | 前年度の行事一覧を読み取り専用で参考表示する |

### 6.5 勤務表作成

| No | 機能 | 内容 |
| --- | --- | --- |
| 6.5.1 | 対象期間の自動入力 | 選択中の処理期の開始日・終了日を自動的に入力する |
| 6.5.2 | 指定日の自動抽出 | 対象期間から土曜・日曜・祝日・年末年始閉庁日を抽出する。確定済み履歴に含まれる日は除く |
| 6.5.3 | 抽出0件時の案内 | 抽出結果が0件の場合、その理由（全て確定済み／対象日なし）を表示する |
| 6.5.4 | 指定日の除外 | 抽出された日のうち不要な日をチェックボックスで対象から外す |
| 6.5.5 | 勤務表の自動作成 | 第8章の割当ルールに基づき、各指定日に係長級1名・主事級1名を割り当てる |
| 6.5.6 | 状態の表示 | 各日を「OK」または「要確認：（理由）」として表示する。要確認の行は強調表示する |
| 6.5.7 | 手動調整 | 各日の係長級・主事級をプルダウンで変更する。変更した日は「（手動で修正済み）」として扱う |
| 6.5.8 | 未割当職員の表示 | 結果一覧の下に、今期まだ一度も割り当てられていない対象職員と、その理由を一覧表示する |
| 6.5.9 | 確定・履歴に保存 | 作成内容を確定し、処理期に紐づけて履歴に保存する |
| 6.5.10 | CSV出力 | 作成内容をCSVで出力する |
| 6.5.11 | PDF出力 | 作成内容をPDFで出力する |

### 6.6 履歴・確認

| No | 機能 | 内容 |
| --- | --- | --- |
| 6.6.1 | 確定済み履歴の表示 | 確定済みの勤務表を一覧表示する。表示フィルタの既定は選択中の処理期。「全期間」「未分類」「各処理期」に切り替えられる |
| 6.6.2 | 変更届の反映 | 該当日の係長級／主事級について、交代後の職員と申請日時を登録する |
| 6.6.3 | 変更届のOCR読取 | 変更申請のスクリーンショット画像から交代相手氏名・申請日時・変更する日付を読み取り、入力欄に下書きする。読取結果は確認・修正のうえ反映する |
| 6.6.4 | OCRの日付照合 | 読み取った「変更する日付」が対象日と異なる場合、警告を表示する |
| 6.6.5 | 履歴の削除 | 指定した日の履歴を削除する |
| 6.6.6 | 担当実績の確認 | 担当実績のある職員の担当回数・直近の勤務日を一覧表示する。担当回数の少ない順に並べる |
| 6.6.7 | 勤務日の詳細表示 | 「履歴を見る」で、その職員が担当した全ての日付と役割を表示する |
| 6.6.8 | Excel勤務実績の取込 | 過去の日直勤務表（.xlsx）を取り込み、選択中の処理期に紐づけて履歴に登録する。同じ日付が既にある場合は上書きする |
| 6.6.9 | 履歴専用職員の自動登録 | 取込時、名簿にない職員番号は履歴専用の職員として登録し、担当実績の確認に利用できるようにする |
| 6.6.10 | CSV出力 | 全履歴をCSVで出力する |
| 6.6.11 | PDF出力 | 全履歴をPDFで出力する |
| 6.6.12 | 引継ぎ用Excel出力 | 全履歴を、勤務実績取込で読み込める形式のExcelで出力する |
| 6.6.13 | バックアップの作成 | 全データを1つのJSONファイルに書き出す |
| 6.6.14 | バックアップからの復元 | JSONファイルから全データを復元する。旧形式のバックアップにも対応する |

---

## 7. 入出力仕様

### 7.1 Excel名簿の取込

人事給与システムで「在職・写真無・詳細」の条件で検索し、結果を Ctrl+A で全選択して任意のExcelに貼り付けたファイルを取り込む。

**必須列**

| 列名 | 用途 |
| --- | --- |
| 番号 | 職員番号（同一人物の判定キー） |
| 氏名 | 氏名 |

**任意列**

| 列名 | 用途 |
| --- | --- |
| 職名 | 参考情報。ランクが読み取れない場合の級判定にも使用 |
| 所属CD | 名簿の表示順（昇順） |
| 所属名 | 所属課。同一課回避・課除外ルール・市民課経験の自動判定に使用 |
| 性別 | 同性ペアの判定に使用 |
| 係名 | 秘書係の自動判定に使用 |
| 兼職 | 運転手の自動判定に使用 |
| 区分名 | 会計年度任用職員・派遣の自動除外に使用 |
| ランク | 級の自動判定に使用 |
| 採用日 | 新規採用職員の除外判定に使用 |
| 退職日 | 退職者の自動除外に使用 |
| 在職 | 非在職者の自動除外に使用 |

**判定ルール**

| 項目 | ルール |
| --- | --- |
| 級（ランクによる） | 1〜399：課長級以上として除外／400〜600：係長級／601〜999：主事級／1000以降：会計年度任用職員等として除外 |
| 級（職名による） | ランクが読み取れない行のみ、職名ごとに「係長級／主事級／取り込まない」を画面上で指定する。指定内容は次回以降の取込にも記憶する |
| 性別 | 1または「男」＝男性／0または「女」＝女性／その他は未設定 |
| 除外 | 区分名が「会計年度任用職員」「派遣」の行、退職日が設定されている行、在職フラグが0の行は取込対象外 |

### 7.2 育休一覧Excelの取込

人事給与システムの「休業情報」で、検索条件に「該当年の1/1以降」「終了」を指定して検索した結果を取り込む。

| 列名 | 必須 | 用途 |
| --- | --- | --- |
| 職員番号 | ○ | 対象職員の特定 |
| 開始 | ○ | 除外期間の開始日 |
| 終了 | | 除外期間の終了日（空欄の場合は復帰まで対象外） |
| 氏名 | | 参考表示 |
| 区分 | | 参考表示 |

### 7.3 産休一覧Excelの取込

人事給与システムの「出産情報」で、検索条件に「該当年の1/1以降」「産前休暇」を指定して検索した結果を取り込む。除外期間は産前休暇〜産後休暇とする。

| 列名 | 必須 | 用途 |
| --- | --- | --- |
| 職員番号 | ○ | 対象職員の特定 |
| 産前休暇 | ○ | 除外期間の開始日 |
| 産後休暇 | | 除外期間の終了日（空欄の場合は復帰まで対象外） |
| 氏名 | | 参考表示 |

### 7.4 Excel勤務実績の取込

本アプリの「引継ぎ用に書き出す」で出力した形式、または同等の列構成のExcelを取り込む。

| 列 | 内容 |
| --- | --- |
| 日付（「月」または「日付」を含む列名） | 対象日 |
| 職員番号（1つ目） | 係長級の職員番号 |
| 氏名（1つ目） | 係長級の氏名 |
| 変更日時（1つ目） | 係長級の変更日時 |
| 職員番号（2つ目） | 主事級の職員番号 |
| 氏名（2つ目） | 主事級の氏名 |
| 変更日時（2つ目） | 主事級の変更日時 |

### 7.5 出力

| 出力 | 形式 | 内容 |
| --- | --- | --- |
| 勤務表CSV | CSV（UTF-8 BOM付） | 日付・曜日・祝日等・係長級・主事級・状態 |
| 履歴CSV | CSV（UTF-8 BOM付） | 期間・日付・曜日・係長級・変更日時・主事級・変更日時・状態 |
| 勤務表PDF／履歴PDF | PDF（A4縦） | 日付・曜日・係長級・変更日時・主事級・変更日時 |
| 引継ぎ用Excel | .xlsx | 月日・曜日・職員番号・氏名・変更日時（係長級／主事級）・状態・期間 |
| バックアップ | JSON | 全データ |
| 名簿CSV | CSV（UTF-8 BOM付） | 番号・氏名・級・職名・所属課・係名・市民課経験・採用年月日 |

---

## 8. 割当ルール仕様

### 8.1 指定日の抽出

以下のいずれかに該当する日を指定日として抽出する。

| 種別 | 判定 |
| --- | --- |
| 土曜・日曜 | 曜日による判定 |
| 祝日 | 計算式による自動判定（春分・秋分は近似式、ハッピーマンデー・振替休日・国民の休日を含む。1980〜2099年に対応） |
| 年末年始閉庁日 | 12/28〜1/3（曜日を問わない） |

### 8.2 ペアの構成

各指定日に、**係長級1名・主事級1名**を割り当てる。

### 8.3 割当ルール一覧

| No | ルール | 内容 | 緩和 |
| --- | --- | --- | --- |
| 8.3.1 | 同性ペア | ペアは同性で組む | 緩和しない |
| 8.3.2 | 女性優先（枯渇ベース） | 女性を優先して割り当て、その日に条件を満たす女性候補（係長級・主事級のいずれか）が枯渇している場合のみ男性に切り替える | 緩和しない |
| 8.3.3 | 同一処理期内1人1回 | 同一処理期内は原則1人1回とし、その期でまだ割り当てていない職員を優先する。該当レベル・性別の未割当候補が枯渇した場合に限り2回目の割当を許可し「要確認」とする | 候補枯渇時のみ |
| 8.3.4 | 育休・産休等の除外 | 登録された除外期間中の職員は割り当てない | 緩和しない |
| 8.3.5 | 常時除外 | 派遣・7割措置・秘書係・運転手・常時除外所属の職員は割り当てない | 緩和しない |
| 8.3.6 | 課除外（月別） | その月に除外指定されている所属の職員は割り当てない | 緩和しない |
| 8.3.7 | 課除外（行事別） | 行事の除外期間（リードタイムを含む）中は、担当課の職員を割り当てない | 緩和しない |
| 8.3.8 | 新規採用職員の除外 | 採用日から6ヶ月（設定可）を経過していない職員は割り当てない | 緩和しない |
| 8.3.9 | 最低間隔日数 | 前回勤務日から120日（設定可）以上の間隔を空ける | 緩和しない |
| 8.3.10 | 年末年始・GWの重複回避 | 年末年始・ゴールデンウィークは、過去の同期間（既定で直近2回）と同じ職員を避ける | 緩和しない |
| 8.3.11 | ペア重複回避 | 過去2年度（設定可）以内に組んだペアは避ける。ただし変更届による交代で成立した組合せは判定対象外とする | 第2段階で緩和 |
| 8.3.12 | 同一課の回避 | 係長級と主事級が同じ課にならないようにする | 第3段階で緩和 |
| 8.3.13 | 資格要件 | ペアのうち少なくとも1名が係長級または市民課経験者であること | 第4段階で緩和 |

### 8.4 段階的緩和

すべての制約を満たす組合せが見つからない場合、以下の順に制約を緩和して再探索する。

| 段階 | 同一課回避 | ペア重複回避 | 資格要件 | 状態 |
| --- | --- | --- | --- | --- |
| 第1段階 | 適用 | 適用 | 適用 | OK |
| 第2段階 | 適用 | 緩和 | 適用 | 要確認：過去のペアと重複しています |
| 第3段階 | 緩和 | 緩和 | 適用 | 要確認：同一課の組合せになっています |
| 第4段階 | 緩和 | 緩和 | 緩和 | 要確認：資格要件を満たす職員がいません |
| 最終手段 | — | — | — | 各候補の先頭同士を組み合わせる |

いずれの段階でも組合せが得られない場合、その日は割当なしとし「要確認：（候補が枯渇している旨）」を表示する。

### 8.5 候補の優先順位

各レベル・性別の候補は、以下の順に並べて先頭から評価する。

1. 資格要件を満たす職員を優先
2. 累計担当回数の少ない職員を優先
3. 前回勤務日が古い職員を優先（担当履歴のない職員を最優先）

累計担当回数による並べ替えにより、ある処理期で割り当てられなかった職員は次の処理期で優先される。

### 8.6 市民課経験の判定

以下のいずれかに該当する場合、市民課経験ありとする。

1. 名簿上で「市民課経験」がオンになっている
2. 現在の所属名に「市民課」を含む
3. 所属履歴に「市民課」を含む所属がある

なお、Excel名簿の取込時に上記2に該当する職員は「市民課経験」が自動的にオンになり、以後の処理期にも引き継がれる。

### 8.7 未割当職員の理由判定

今期まだ割り当てられていない対象職員について、対象日ごとに以下を積み上げて理由を表示する。

| 理由 | 判定 |
| --- | --- |
| 性別が未設定 | 性別が設定されていない |
| 育休・産休等の除外期間中 | 対象日が登録された除外期間内 |
| 所属の除外ルールに該当 | 対象日に所属が月別・行事別の除外対象 |
| 採用から6ヶ月未満 | 対象日が採用日＋6ヶ月より前 |
| 前回勤務日から120日未満 | 対象日が前回勤務日＋120日より前 |
| 今期の割当枠が埋まった | 候補ではあったが、他の職員が優先された |
| 異性でペアが組めた日 | その日は他方の性別でペアが成立した |

本機能は完全なシミュレーションではなく、職員個人の属性による除外の積み上げによる推定である。同一課回避・ペア重複回避などペア単位の制約は理由に反映せず、「今期の割当枠が埋まった」に含まれる。

---

## 9. 非機能要件

### 9.1 動作環境

| 項目 | 要件 |
| --- | --- |
| 端末 | 庁内PC（インターネット接続不要） |
| ブラウザ | Google Chrome、Microsoft Edge 等の最新のモダンブラウザ |
| 画面解像度 | 1024×768以上を推奨 |
| インストール | 不要（ファイル一式を任意のフォルダに配置） |

### 9.2 性能

| 項目 | 要件 |
| --- | --- |
| 勤務表作成時間 | 半期分（約60日・職員数百名規模）で数秒以内 |
| OCR読取時間 | 1枚あたり数秒〜十数秒 |

### 9.3 セキュリティ

| 項目 | 要件 |
| --- | --- |
| 外部通信 | 行わない。全てのライブラリを同梱し、CDN等を参照しない |
| データ保存先 | 利用端末のブラウザ localStorage のみ |
| 個人情報の取扱い | 職員名簿・勤務実績を保持するため、共用端末では使用しない |
| 認証 | なし（端末の利用者管理に依存する） |

### 9.4 保守性

| 項目 | 要件 |
| --- | --- |
| データ移行 | 旧バージョンのデータ構造から自動的に移行する |
| バックアップ | 全データをJSONファイルで書き出し・復元できる |
| 引継ぎ | 履歴をExcelで書き出し、後任者の端末へ取り込める |

---

## 10. 制約事項

| No | 制約 |
| --- | --- |
| 10.1 | データはブラウザ（端末）ごとの localStorage に保存されるため、他の端末とは共有されない |
| 10.2 | ブラウザの閲覧データを削除するとデータが消去される。定期的なバックアップが必要 |
| 10.3 | 祝日は計算式による自動判定であり、法改正や臨時の祝日には対応しない。作成後の確認が必要 |
| 10.4 | 自動割当は補助であり、「要確認」の日や組織の実情に応じた調整は手動で行う |
| 10.5 | PDF出力は画面表示を画像化する方式のため、PDF内の文字はコピーできない |
| 10.6 | 変更届のOCR読取機能は、`python3 -m http.server` 等でファイルを配信して開いた場合のみ使用できる。`index.html` を直接開いた場合（`file://`）はブラウザの制約により使用できない。この場合も手入力での交代反映は使用できる |
| 10.7 | OCRの読取結果は誤読することがある（特に人名の漢字）。必ず内容を確認してから反映する |
| 10.8 | 性別はExcel名簿の取込時にのみ自動設定される。個別登録した職員は性別が未設定となり、割当対象にならない |
| 10.9 | Excel名簿の取込は選択中の処理期の名簿を全置換するため、個別登録した職員も置換対象となる |

---

## 11. ルールの出典

本システムが実装する割当ルールは、「日直勤務表の作成及び変更等についてのマニュアル」および追加仕様確認書に基づく。

| No | ルール |
| --- | --- |
| 1 | 補佐級以下が対象。外局・秘書・運転手・会計年度任用職員・派遣・7割措置・育休/産休等の期間中の職員は除く。新規採用職員は採用から6ヶ月経過後から対象 |
| 2 | 係長級と主事級の組合せが基本。日直のペアは同性とし、女性を優先して割り当て、その日に条件を満たす女性の候補が枯渇している場合のみ男性に切り替える。毎日の組合せのうち少なくとも1名は係長級または市民課経験者であること。同じ職場同士は組み合わせない |
| 3 | 各行事の除外期間（リードタイムを含む）は担当課を除外する。10・11月は商工観光課・広報係、12・1月はふるさと納税係、1〜4月は税務課を除外 |
| 4 | 年末年始（12/28〜1/3）・ゴールデンウィークは前回・前々回と同じ職員にならないよう配慮する |
| 5 | 係長級・主事級の組合せ（ペア）は過去2年度以内の重複を避ける。ただし変更届による交代は対象外とする |
| 6 | 前回勤務日から120日以上間隔を空ける |
| 7 | 同一処理期内は原則1人1回の割当とする（該当する候補が枯渇した場合に限り、その期2回目の割当を許可する） |

---

## 12. 改訂履歴

| 版数 | 日付 | 改訂内容 |
| --- | --- | --- |
| 1.0 | 2026-08-19 | 初版作成 |
