# CLAUDE.md

このリポジトリで作業するときの約束事です。**作業を始める前に必ず読んでください。**
これまでの経緯と未決事項は `docs/作業記録_2026-09.md` にあります。

---

## プロジェクト概要

市役所の**日直勤務表（土日祝日・年末年始の当番表）を自動作成するアプリ**です。

- ブラウザだけで動く。サーバー不要、データは **localStorage のみ**（端末の外に出ない）
- `index.html` をダブルクリック（`file://`）でも、`python3 -m http.server` 経由（`http://`）でも使われる
  → **どちらの開き方でも動くこと**が必須
- 利用者は市役所の職員。**画面の文言・ドキュメントはすべて日本語**

| ファイル | 役割 |
| --- | --- |
| `app.js` | 割当ロジック（DOMに依存しない。Nodeから `require` できる） |
| `ui.js` | 画面の処理すべて（約4600行） |
| `index.html` / `style.css` | 画面。「使い方」タブの説明文も `index.html` にある |
| `vendor/` | jsPDF・SheetJS・Tesseract.js（外部CDNは使わない＝オフラインで動かすため） |
| `docs/仕様書.md` / `docs/設計書.md` | 仕様の正本。**アプリ内の「仕様書・設計書」タブにも表示される** |
| `docs-content.js` | 上の2つを埋め込んだ**自動生成物**（直接編集しない） |
| `docs/manual/` | 利用者向けマニュアル2冊の原稿（下記） |
| `tests/` | 回帰テスト |

---

## 利用者との進め方（重要）

利用者は**実装の前に画面で確認してから承認する**進め方を好みます。

- 「実装まだ」「実装しないで」と言われたら、**本体のファイルは触らない**。
  作業用フォルダにアプリを丸ごとコピーして試作し、**画面のスクリーンショットを見せて**承認を待つ
- 「実装して」「実装しましょう」で初めて本体に反映する
- 迷う点は、案を示したうえで確認する。勝手に仕様を広げない
- 依頼された範囲だけを変える。**頼まれていない改善を足しすぎない**
  （例：NotebookLM資料の「文字化けだけ直して」に対し、体裁まで作り変えて差し戻しになった）

---

## 変更したら必ずやること

1. **テストを通す** — `node tests/run_all.js`（全件成功が必須）
2. 新しい機能・直した不具合には、**テストを足す**（`tests/*.test.js`）
3. **仕様書を更新する** — `docs/仕様書.md` の該当節と、末尾「12. 改訂履歴」に1行追加
   （版番号は直前の続き。現在の最新は **1.48**）
4. **`node build_docs.js`** を実行して `docs-content.js` を作り直す（忘れるとアプリ内の仕様書が古いまま）
5. 画面の操作が変わったら、`index.html` の「使い方」タブの説明と、マニュアル（`docs/manual/`）も直す
6. コミット・プッシュ（ブランチ `claude/duty-roster-automation-app-qhozys`）

### コミットメッセージ

日本語で、1行目に要約、本文に**なぜそうしたか**を書く。既存のコミットに倣う。

---

## テスト

```bash
node tests/run_all.js      # すべて実行（ポート8899でアプリを自動配信して、終わったら止める）
node tests/change.test.js  # 1本だけ（先に python3 -m http.server 8899 が必要）
```

| テスト | 見ていること |
| --- | --- |
| `logic.test.js` | 割当ルール（係長級の有無・同一課・男女・1人1回・性別ゾーン・最適化・変更届の読み取り） |
| `tabs.test.js` | 7タブが `http://` と `file://` の両方で開き、JSエラーが出ない |
| `generate.test.js` | 作成 → 決裁チェック → 確定 → ロック・保持 → 引き戻し、手動変更時のルール違反通知 |
| `change.test.js` | 交代を反映（交換の解釈・警告と確認・行違い・ルール違反の通知、`file://` でも） |
| `changelog_backup.test.js` | 交代の一覧（4分割・「―」・フィルタ連動）、バックアップの促し・件数・書出・復元 |

テストデータは `tests/fixtures.js`（架空の職員）。Playwright は `/opt/node22/lib/node_modules/playwright`、
Chromium は `/opt/pw-browsers/chromium` を使う。

---

## 業務ルールで間違えやすいところ

- **変更届の「変更する日付」は、交代相手が日直に入っている日**（＝申請者がこれから代わりに入る日）。
  申請者自身の担当日ではない。この解釈を誤ると、交代相手が二重に入り申請者がどこにも入らなくなる
- 「交代を反映」は**申請者本人の行**から開く。別人の行に貼ると、入力欄を埋めずに正しい行を案内する
- 1日2名のうち**少なくとも1名が係長級**（1人目は必ず係長級）。係長級2名もあり。
  ただし課長補佐・副主幹どうしの組合せは避ける
- ペアは**同性**。女性を優先し、女性が尽きた日から男性へ**一方向**に切り替わる（戻らない）
- 同一処理期内は**原則1人1回**。尽きた場合のみ2回目を「要確認」として許す
- 詳細は `docs/仕様書.md` 8章

---

## マニュアル（`docs/manual/`）

| PDF | 読む人 | 作り直し方 |
| --- | --- | --- |
| `docs/日直勤務表アプリ_操作マニュアル.pdf` | 勤務表の担当者 | `node docs/manual/build_manual.js` |
| `docs/日直勤務変更_申請手順.pdf` | 申請する職員 | `python3 docs/manual/annotate.py` → `node docs/manual/build_apply.js` |

- 操作マニュアルの画面写真は `docs/manual/shots.js` がアプリを実際に操作して撮る（架空データ）
- **Garoon（グループウェア）の画面は、必ず利用者からもらった実物のスクリーンショットを使う。**
  生成AIで描かせたり、描き起こしたりしない（実在しないメニューや誤った氏名が入る）
- 申請手順は **NotebookLM版の体裁（16:9・暗転＋オレンジの矢印）を維持する**のが利用者の意向
- 詳しくは `docs/manual/README.md`

---

## 画面・文章の書き方

- ヒント文で「利用者がすること」は `<strong class="hint-do">` で黒太字にする
- 特に目立たせる注意は `.em-red`（太赤字）。本当のエラー表示（赤枠）とは見た目を分ける
- 新卒1年目でも迷わない言葉で書く。専門用語は言い換えるか説明を添える

---

## この環境で踏んだ落とし穴

| 事象 | 対処 |
| --- | --- |
| **作業用フォルダ（scratchpad）はセッションをまたぐと消える** | 残すべきものは必ずリポジトリに入れる（一度テスト10本を失った） |
| Playwright の `setInputFiles` は**日本語ファイル名を無視する** | ASCII名で保存してから渡す |
| `select.value = x` では `change` が発火しない | `dispatchEvent(new Event('change'))` を明示する |
| 結果表のプルダウンは変更のたびに描き直される | 要素は操作の直前に取り直す（古いハンドルは無効） |
| `page.screenshot({clip})` はビューポートの外が切れる | `scrollIntoView({block:'center'})` してから撮る |
| `pkill -f "http.server 8899"` が**自分のシェルにも一致して**終了コード144になる | `ps aux \| grep "[h]ttp.server 8899"` でPIDを取って `kill` する |
| ChromiumはPDF埋め込み時に画像を作り直す | 元PNGを圧縮してもPDFは小さくならない。撮影時の倍率で調整する |
| チャットに**貼り付けた画像**はファイルとして届かない | 利用者には添付（WordやPDFにまとめても可）で送ってもらい、中の画像を取り出す |
| poppler が無くPDFを描画できない | HTML側を印刷メディアで画像化して確認するか、zlib＋PILで画像を取り出す |
